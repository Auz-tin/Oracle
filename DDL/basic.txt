-- TABLE NAME: STUDENT

-- DESCRIBE TABLE (column names, column datatype)
DESC student;

-- CREATE TABLE 
CREATE TABLE student (
name varchar(20),
age number(2),
fees number(8,2),
);

CREATE TABLE student (
name varchar(20) PRIMARY KEY,
age number(2),
fees number(8,2) REFERENCES table2
);

CREATE TABLE student (
name varchar(20),
age number(2),
fees number(8,2)
CONSTRAINT constraint_name PRIMARY KEY (name,age),
CONSTRAINT constraint_name PRIMARY KEY (name,age),
);


-- INSERT VALUES INTO THE TABLE
INSERT INTO studenttable  VALUES ( 'SAM',19,7888.90);
INSERT INTO studenttable (age,fees,name)  VALUES ( 21,5628.90,'HAM');
